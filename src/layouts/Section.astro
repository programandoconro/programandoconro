---
interface Props {
  title?: string;
  id?: string;
}

const { title, id } = Astro.props as Props;
---

<section>
  <p class="banner" id={id}>{title}</p>
  <slot />
</section>

<style>
  .banner {
    line-height: 1.6;
    margin: 1rem 0;
    background: #2c1d9b;
    padding: 1rem;
    border-radius: 0.4rem;
    color: var(--astro-white);
  }
</style>
